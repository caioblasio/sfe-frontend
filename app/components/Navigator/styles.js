import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  beforeButton: {
    textAlign: "left",
  },
  nextButton: {
    textAlign: "right",
  },
}));

export default useStyles;
